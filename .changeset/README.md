# Changesets

Add one Markdown file here for each user-visible package change. Use `pnpm changeset` interactively, or create a file with package names, bump levels, and a concise release note. The fixed group keeps the published panda packages on one version.
